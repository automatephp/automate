strategy: sftp # or git

sftp:
    exclude_folders:
        - ".git"
        - ".idea"
    local_build:
        - "php -v"

repository: git@gitlab.eolas.fr:romaric.paul/test.git

plugins:
    cache_tool:
        opcache: true

platforms:
    development:
        default_branch: master
        max_releases: 3
        servers:
            10.66.236.233:
                host: 10.66.236.233
                user: responsage
                password: Taef4que
                path: /home/wwwroot/responsage/romaricp

pre_deploy:
    - "php -v"
